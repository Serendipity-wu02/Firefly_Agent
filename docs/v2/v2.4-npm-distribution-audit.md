# Firefly-Agent V2.4: npm Registry 分发层建设与审计报告 (npm Distribution Audit)

- **版本基线**: Firefly-Agent V2.4 (Development)
- **审计日期**: 2026-09-01
- **核心目标**: 建立符合 npm Registry 正式发布标准的 npm Package 与 CLI Launcher 分发层，实现从干净环境执行 `npm install -g firefly-agent` 后直接通过 `firefly` 启动桌面应用。
- **状态**: AUDIT & DRY-RUN COMPLETE (PASS)

---

## 1. npm Package 名称
- **Package Name**: `firefly-agent`

---

## 2. npm Registry 当前占用情况
- **查询指令**: `npm view firefly-agent version`
- **实际响应**:
  ```text
  npm error code E404
  npm error 404 Unpublished on 2026-04-23T00:58:10.207Z
  npm error 404 The requested resource 'firefly-agent' could not be found or you do not have permission to access it.
  ```
- **占用判定**: 当前包名在 npm Registry 上处于 Unpublished 状态（可供本项目正式发布接入）。

---

## 3. npm CLI Bin 名称
在 `package.json` 中配置双向 CLI 命令别名：
- **`firefly`**: `./bin/firefly.mjs` (简短标准命令)
- **`firefly-agent`**: `./bin/firefly.mjs` (完整包名同名命令)

---

## 4. package.json 最终发布字段
```json
{
  "name": "firefly-agent",
  "version": "2.3.0",
  "description": "Firefly Desktop AI Agent - 流萤桌面智能体",
  "main": "dist/main/main/index.js",
  "bin": {
    "firefly": "./bin/firefly.mjs",
    "firefly-agent": "./bin/firefly.mjs"
  },
  "files": [
    "bin",
    "dist",
    "assets",
    "resources",
    "config/settings.example.json",
    "data/knowledge",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=11.0.0"
  },
  "packageManager": "npm@11.17.0",
  "scripts": {
    "build:main": "tsc -p tsconfig.main.json",
    "build:preload": "tsc -p tsconfig.preload.json",
    "build:renderer": "vite build",
    "build": "npm run build:main && npm run build:preload && npm run build:renderer",
    "dev": "npm run build:main && npm run build:preload && concurrently \"vite\" \"cross-env VITE_DEV=1 electron .\"",
    "start": "electron .",
    "typecheck": "tsc -p tsconfig.main.json --noEmit && tsc -p tsconfig.preload.json --noEmit",
    "test": "node tools/test_environment_baseline.mjs && node tools/test_npm_distribution.mjs && node tools/test_firefly_agent_core_freeze.mjs && node tools/test_firefly_agent_core_v1.mjs && node tools/test_firefly_voice_v1.mjs && node tools/test_llm_tts_live2d_chain.mjs && node tools/test_music_system.mjs && node tools/test_qqmusic_desktop_bridge.mjs && node tools/test_v2_phase_a_context.mjs && node tools/test_v2_phase_b_compaction.mjs && node tools/test_v2_phase_c_tool_policy.mjs && node tools/test_v2_phase_d_recovery_durability.mjs && node tools/test_v2_phase_e_bounded_planning.mjs && node tools/test_v2_memory_phase1_store.mjs && node tools/test_v2_memory_phase2_retrieval.mjs && node tools/test_live2d_only.mjs && node tools/test_v2_memory_phase3_write_policy.mjs && node tools/test_v2_memory_phase4_evolution.mjs && node tools/test_v2_memory_phase5_context_integration.mjs && node tools/test_v2_rag_phase1_ingestion.mjs && node tools/test_v2_rag_phase2_vector_store.mjs && node tools/test_v2_rag_phase3_hybrid_retrieval.mjs && node tools/test_v2_rag_production_embedding.mjs && node tools/test_v2_rag_phase4_ragslot_integration.mjs && node tools/test_live2d_resource_reload.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3",
    "typescript": "^5.6.3",
    "vite": "^6.0.1"
  },
  "dependencies": {
    "electron": "^33.2.0",
    "js-yaml": "^4.1.0",
    "@types/js-yaml": "^4.0.9",
    "pixi.js": "^7.4.2",
    "pixi-live2d-display": "0.5.0-beta",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "lucide-react": "^0.468.0"
  },
  "author": "Serendipity-wu02",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Serendipity-wu02/Firefly_Agent.git"
  },
  "keywords": [
    "firefly",
    "desktop-pet",
    "ai-agent",
    "live2d",
    "electron",
    "honkai-star-rail"
  ]
}
```

---

## 5. `files` 发布白名单
```json
"files": [
  "bin",
  "dist",
  "assets",
  "resources",
  "config/settings.example.json",
  "data/knowledge",
  "README.md",
  "LICENSE"
]
```

### 明确排除的开发/内部文件
- `src/` (TypeScript 源代码)
- `tools/` (测试脚本与开发工具)
- `docs/` (内部设计与审计文档)
- `*.py` / `__pycache__/` (历史 Python 支撑脚本)
- `tsconfig*.json` / `vite.config.ts`
- `.git/` / `.github/` / `.venv/` / `node_modules/`
- `.node-version` / `.nvmrc` / `.npmrc` / `.ignore` / `.gitignore`

---

## 6. npm tarball 文件清单统计
- **打包包含总文件数**: **1,130** 个文件
- **核心文件分布**:
  - `resources/`: 806 个 Canonical 知识语料与人设文件
  - `data/knowledge/`: 5 个离线切片与向量索引文件 (`chunks.json`, `vector_index.json` 等)
  - `assets/firefly/`: Live2D 模型、表情、动作与音频资源
  - `dist/`: 编译后的 main, preload, renderer 产物
  - `bin/`: CLI 入口 (`bin/firefly.mjs`)
  - `config/`: `settings.example.json`

---

## 7. tarball 总大小
- **压缩包大小 (Package Size)**: **28.4 MB**
- **解压后大小 (Unpacked Size)**: **114.5 MB**
- **校验和 (SHA-512)**: 自动生成并验证

---

## 8. npm publish --dry-run 结果
- **执行指令**: `npm publish --dry-run`
- **结果**:
  ```text
  npm notice Publishing to https://registry.npmjs.org/ with tag latest and default access (dry-run)
  + firefly-agent@2.3.0
  ```
- **判定**: `PASS (0 Errors)`

---

## 9. 干净目录安装测试结果 (Clean Directory Install)
通过自动化测试套件 `tools/test_npm_distribution.mjs`：
1. 本地打包生成 `firefly-agent-2.3.0.tgz`；
2. 在操作系统临时目录创建全新孤立目录（无当前仓库代码）；
3. 执行 `npm install firefly-agent-2.3.0.tgz --no-audit --no-fund`；
4. 验证 `node_modules/firefly-agent/` 中包含所有必要运行时文件，且绝对不包含 `src/`, `tools/`, `docs/`, `*.py` 等开发文件；
5. **结果**: **PASS**。

---

## 10. 安装后 CLI 执行结果
在完全孤立的安装目录中执行：
```powershell
node ./node_modules/firefly-agent/bin/firefly.mjs --version
# 输出: firefly-agent v2.3.0

node ./node_modules/firefly-agent/bin/firefly.mjs --help
# 输出完整 CLI 帮助信息与用法说明
```
- **结果**: **PASS**。

---

## 11. Electron 路径解析结果 (Path Isolation)
- **应用根路径解析**: CLI 自动计算 `packageRoot = path.resolve(__dirname, '..')` 并作为 Electron 启动根目录；
- **资源相对解析**: 主进程统一通过 `app.getAppPath()` 解析 `dist/`, `assets/`, `resources/`, `data/knowledge/`；
- **工作目录无关性**: 用户在任意目录（如 `C:\Users\Desktop`）下执行 `firefly` 命令，Electron 均能准确定位 npm 包安装目录中的资源，杜绝 `process.cwd()` 污染。

---

## 12. Node.js 24 / npm 11 结果
- **Node.js**: `v24.19.0`
- **npm**: `11.17.0`
- **CLI 前置检查**: `bin/firefly.mjs` 内置 Node.js >= 24 严格断言。

---

## 13. Typecheck
```powershell
node tools/npm.mjs run typecheck
```
- **结果**: **0 Errors (PASS)**

---

## 14. Build
```powershell
node tools/npm.mjs run build
```
- **结果**: **Main, Preload, Vite Renderer Clean Build (PASS)**

---

## 15. npm test
```powershell
node tools/npm.mjs test
```
- **套件数量**: 25 个测试套件
- **用例总数**: 307 项单元与集成测试
- **结果**: **307/307 PASS (100%)**

---

## 16. Git diff
```diff
diff --git a/package.json b/package.json
index d735874..f2e690c 100644
--- a/package.json
+++ b/package.json
@@ -3,6 +3,25 @@
   "version": "1.0.0",
   "description": "Firefly Desktop AI Agent - 流萤桌面智能体",
   "main": "dist/main/main/index.js",
+  "bin": {
+    "firefly": "./bin/firefly.mjs",
+    "firefly-agent": "./bin/firefly.mjs"
+  },
+  "files": [
+    "bin",
+    "dist",
+    "assets",
+    "resources",
+    "config/settings.example.json",
+    "data/knowledge",
+    "README.md",
+    "LICENSE"
+  ],
+  "engines": {
+    "node": ">=24.0.0",
+    "npm": ">=11.0.0"
+  },
+  "packageManager": "npm@11.17.0",
...
```

---

## 17. Git status
```text
On branch main
Changes not staged for commit:
	modified:   DEVELOPMENT.md
	modified:   README.md
	modified:   package.json

Untracked files:
	.node-version
	.npmrc
	.nvmrc
	bin/
	docs/v2/v2.4-character-foundation-audit.md
	docs/v2/v2.4-npm-distribution-audit.md
	setup.bat
	tools/bin/
	tools/npm.mjs
	tools/test_environment_baseline.mjs
	tools/test_npm_distribution.mjs
```
- `resources/` 物理目录 806 个语料文件：**`git diff -- resources/` 严格为 0**；
- 未执行真实 `npm publish`，未进入 V2.4 Character Runtime 建设。
